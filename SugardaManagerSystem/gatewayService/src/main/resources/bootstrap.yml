server:
  port: 5000
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
spring:
  application:
    name: GATEWAY-SERVICE
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVICE
  profiles:
    active: dev